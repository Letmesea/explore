<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.letmesea.doit.dao.DealDao">
    <select id="getData" resultType="java.lang.String">
        select left(win_number,17) from t_ssq order by open_dtm desc ;
    </select>
    <select id="getKjData" parameterType="integer" resultType="java.lang.String">
        select win_number from t_ssq order by open_dtm desc limit #{item};
    </select>
    <select id="getSsqAllData" resultType="java.lang.String">
        select tsa.number from t_ssq_all tsa;
    </select>
    <insert id="batchInsertSsq" parameterType="java.util.List">
        <selectKey resultType ="java.lang.Integer" keyProperty= "number"
             order= "AFTER">
            SELECT LAST_INSERT_ID()
        </selectKey >
        insert into t_ssq(number,win_number,sq_number,open_dtm,create_dtm,update_dtm,first_prize_num,total_amount,second_prize_num)
        values
        <foreach collection ="list" item="ssq" index= "index" separator =",">
              (
              #{ssq.number}, #{ssq.winNumber},
              #{ssq.sqNumber},
              #{ssq.openDtm},
              now(), now(),#{ssq.firstPrizeNum},#{ssq.totalAmount},#{ssq.secondPrizeNum}
              )
        </foreach >
    </insert>
    <insert id="batchInsertSsqAll" parameterType="java.util.List">
        <selectKey resultType ="java.lang.Integer" keyProperty= "number"
                   order= "AFTER">
            SELECT LAST_INSERT_ID()
        </selectKey >
        insert into t_ssq_all(number,create_dtm,update_dtm)
        values
        <foreach collection ="list" item="ssq" index= "index" separator =",">
            (
            #{ssq},
            now(), now()
            )
        </foreach >
    </insert>
</mapper>
